[formatter.nix]
command = "nixfmt"
excludes = []
includes = ["*.nix"]
options = []

[formatter.toml-sort]
command = "toml-sort"
excludes = []
includes = ["*.toml"]
options = ["-i"]

[formatter.typos]
command = "typos"
excludes = ["theme"]
includes = ["*"]
options = ["--write-changes"]
